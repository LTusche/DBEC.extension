<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        HorizontalContentAlignment="Center"
        Height="500"
        Width="700"
        Title="Kabelsystem Konfiguration"
        Loaded="On_Loaded">
    <Grid Margin="40,2,50,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Verkabelungsart"/>
        <ComboBox Grid.Column="1" x:Name="WiringTypeBox" SelectedItem="{Binding WiringType}" DisplayMemberPath="Name" IsEditable="False" IsReadOnly="True"/>
    </Grid>
</Window>
